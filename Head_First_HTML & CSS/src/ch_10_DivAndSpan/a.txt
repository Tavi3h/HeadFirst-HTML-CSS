<a>元素的多种状态。
link、visited、hover、focus、active。/*如果要定义所有的状态，推荐使用该顺序*/
link: 链接处于未被访问过的状态。
visited：处于访问过的状态。
hover：鼠标悬停下的状态。
focus：使用tab选择到该链接时的状态。
active：用户第一次单击一个链接时的状态。

使用伪类来指定状态样式。
a:link {
	color: green;
}

a:visited {
	color: red;
}

a:hover {
	color: yellow
}