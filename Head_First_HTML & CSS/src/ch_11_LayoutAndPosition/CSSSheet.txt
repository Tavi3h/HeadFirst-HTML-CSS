CSS表格：
可以把表格想象为一个电子表格，包含行和列。每个单元格内可以放置一个块元素。
表格可以自动扩展来适应单元格宽度和高度。

首先把要作为一个表格显示的所有内容包围在一个<div>中，并命名为tableContainer。display: table;
然后，再创建一个<div>作为行，命名为tableRow。display: table-row;
如果有多行，就创建多个<div>。不过每一行的列数要相同。
把要实现为表格的元素作为单元格。display: table-cell;